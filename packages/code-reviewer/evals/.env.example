# Copy these into src/.env alongside existing vars before running `pnpm eval`.

# Required: OpenRouter key — used by all models, including the LLM-as-judge
OPENROUTER_API_KEY=

# Optional: default model for CLI (pnpm review / pnpm ping) — eval-selected winner
OPENROUTER_MODEL=anthropic/claude-haiku-4.5
